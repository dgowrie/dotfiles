# nvm setup
# last modified 2026.02.18
export NVM_DIR="$HOME/.nvm"
[ -s "$(brew --prefix nvm)/nvm.sh" ] && \. "$(brew --prefix nvm)/nvm.sh"  # This loads nvm
[ -s "$(brew --prefix nvm)/etc/bash_completion.d/nvm" ] && \. "$(brew --prefix nvm)/etc/bash_completion.d/nvm"  # This loads nvm bash_completion

# The following lines have been added by Docker Desktop to enable Docker CLI completions.
fpath=(/Users/dggraf/.docker/completions $fpath)
autoload -Uz compinit
compinit
# End of Docker CLI completions
export PATH="$HOME/.local/bin:$PATH"

# Claude Code defaults: opus 4.6 (1M context) + medium effort. Pass extra flags to override.
claude() {
  command claude --model 'claude-opus-4-6[1m]' --effort medium "$@"
}
alias claude-high='command claude --model "claude-opus-4-7[1m]" --effort xhigh'

# prbatch [claude-args...] - launch Claude Code with a pinned window title.
# Use for Agent View batches of autonomous PR code reviews: disables Claude
# Code's own title management (CLAUDE_CODE_DISABLE_TERMINAL_TITLE) and pins
# the title to "PR Code Reviews" so the window reflects the batch's purpose.
# e.g. prbatch    or    prbatch --resume
prbatch() {
  export CLAUDE_CODE_DISABLE_TERMINAL_TITLE=1
  printf '\033]0;PR Code Reviews\007'
  claude "$@"
}
